<%= form_with model: @book do |f| %>
<% if flash[:notice] %>
  <div class="flash">
    <%= flash[:notice] %>
  </div>
<% end %>


<h2>Title</h2>
<p><%=@book.title %></p>

<h2>Body</h2>
<p><%=@book.body %></p>

<%= link_to "Edit", "/books/#{@book.id}/edit" %>
|
<%= link_to "Back", "/books" %>